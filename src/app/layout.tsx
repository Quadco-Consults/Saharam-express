import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { AuthProvider } from '@/components/AuthProvider'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Saharam Express Limited - Book Your Journey',
  description: 'Premium car logistics service between Kano and Kaduna. Driving excellence one mile at a time.',
  keywords: 'transport, logistics, Kano, Kaduna, Nigeria, booking, tickets',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <AuthProvider>
          {children}
        </AuthProvider>
      </body>
    </html>
  )
}